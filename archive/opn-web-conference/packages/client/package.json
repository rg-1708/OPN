{
  "name": "@opn/client",
  "version": "0.0.0",
  "description": "Framework-agnostic, style-free OPN wire runtime: connect, auth, cmd/ack, refresh, reconnect+resume, dispatch+dedupe.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "node --test test/"
  },
  "dependencies": {
    "@opn/contracts": "0.1.0"
  }
}
