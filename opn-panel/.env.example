# Dev only. Vite proxies /admin to Core's private admin bind (default below).
# Nothing here is a secret — the admin password/JWT live in Core's env, and the
# panel only ever holds a short-lived JWT in browser memory.
ADMIN_BIND_URL=http://127.0.0.1:9091
